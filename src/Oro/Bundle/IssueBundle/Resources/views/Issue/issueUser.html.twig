{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set widgetContent %}
    {{ oro_widget_render({
        'widgetType': 'block',
        'url': path('oro_issue_user_datagrid', { id: entity.id }),
        'title': 'oro.issue.entity_plural_label'|trans
    }) }}
{% endset %}
{{ UI.scrollSubblock(null, [widgetContent]) }}